# Codex Adapter

Use Product OS agent packs as the execution contract.

Flow:

1. Open `agent-packs/<stage>/task.md`.
1. Open `agent-packs/<stage>/context.json`.
1. Follow `codex.prompt.md`.
1. Update only initiative-scoped files.
1. Run `status` and `validate`.
1. Hand off for human approval.
