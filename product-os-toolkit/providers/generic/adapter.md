# Generic LLM Adapter

If no native agent integration exists, execute Product OS manually with the same artifact contract.

Flow:

1. Open `agent-packs/<stage>/task.md`.
1. Open `agent-packs/<stage>/context.json`.
1. Follow `generic.prompt.md`.
1. Produce required artifacts.
1. Run `validate`.
1. Ask PM for approval to continue.
