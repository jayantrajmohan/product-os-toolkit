# Provider Usage

Codex:

- Use `codex.prompt.md` in each stage pack.

Claude Code:

- Use `claude.prompt.md` in each stage pack.

Generic:

- Use `generic.prompt.md` and manual stage review.

All providers must output identical artifact schema and respect stage gates.
